apiVersion: v1
kind: ServiceAccount
metadata:
  name: sequencer-sa
  namespace: sequencer
  labels:
    app.kubernetes.io/name: stateset-sequencer
  annotations:
    # Workload Identity binding for Cloud SQL access
    iam.gke.io/gcp-service-account: sequencer-sa@stateset-network.iam.gserviceaccount.com
