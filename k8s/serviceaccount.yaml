apiVersion: v1
kind: ServiceAccount
metadata:
  name: sequencer-sa
  namespace: sequencer
  labels:
    app.kubernetes.io/name: stateset-sequencer
  annotations:
    # Workload Identity binding for Cloud SQL access (replace with your GSA email).
    iam.gke.io/gcp-service-account: "${GKE_GSA_EMAIL}"
